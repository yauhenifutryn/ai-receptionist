# Kickoff Plan — 2026-05-14

Day 7 of 21. 16 days to Demo Day. Inovo VC pitch in 4 days.

## Decisions locked today

1. **Team lanes**: Jenya = core product, Rem = frontend + integrations, Sebastian = GTM. Rem's concrete scope defined later (Day 8-9) once Jenya has core scaffolded and contract files exist.
2. **Stack simplification**: Lovable out. Next.js + Tailwind + shadcn/ui built directly with Claude Code from Day 1. No refactor step.
3. **No landing page** before Inovo. Demo is the live browser test.
4. **LLM default**: Claude Sonnet 4.6 via Anthropic EU. Polish benchmark in W1.
5. **Backup hosting**: Hetzner Falkenstein.
6. **SMS sender ID**: shared brand for pilots; per-clinic as +200 PLN/mo upgrade.
7. **Ontology shape**: private KB in W1-W2; ElevenLabs Agent Skill in W3 if licensing comes up.
8. **Compliance Add-on**: v2, not now.
9. **Repo**: dedicated git repo inside this directory; private GitHub repo to be pushed.
10. **PR boundary**: `packages/contracts/*` PRs require Jenya's approval. Everywhere else, author self-merges after CI green.
11. **Framework**: superpowers daily; `gsd-ai-integration-phase` once for `docs/AI-SPEC.md`; `gsd-pause-work` / `gsd-resume-work` for session continuity.

## Contract files (the Jenya-Rem sync surface)

Live in `packages/contracts/`. PR-gated. Locked by Day 8.

| File                             | Owner                  | Consumer                 | Purpose                                                                  |
| -------------------------------- | ---------------------- | ------------------------ | ------------------------------------------------------------------------ |
| `scraper.schema.ts`              | Jenya                  | Rem (wizard)             | What `URL → JSON` looks like after the scraper runs.                     |
| `appointment-category.enum.ts`   | Jenya                  | Rem (dashboard ROI math) | Categorical classifier output the agent writes during calls.             |
| `service-value-matrix.schema.ts` | Jenya (agent reads it) | Rem (UI editor)          | Per-clinic price table for ROI calculation.                              |
| `server-tools.contract.ts`       | Jenya (agent calls)    | Rem (adapters implement) | Types for Booksy / Medfile / GCal check-availability and create-booking. |
| `post-call-webhook.schema.ts`    | Both                   | Both                     | What ElevenLabs sends us at call end.                                    |
| `consent-flag.schema.ts`         | Jenya                  | Rem (transcript UI)      | Branch logic gating transcript storage.                                  |
| `voice-agent-provider.ts`        | Jenya                  | Internal                 | `VoiceAgentProvider` interface, ElevenLabs impl today, swappable.        |

## Collaboration model (how Jenya and Rem move in parallel)

- Monorepo, private GitHub.
- Branch: `main` (deployable) ← `dev` (integration) ← `feature/<lane>-<scope>`.
- PR review: required only for `packages/contracts/*`. Everywhere else, self-merge after CI green.
- CI: GitHub Actions, typecheck + lint + tests on every PR; light E2E from W2.
- Code task board: GitHub Projects Kanban on this repo. Three columns: Today / In progress / Done.
- GTM tracker: Sebastian works in Notion. Structured prospect DB with discovery-answer columns. CSV export to `docs/research/` weekly if Jenya wants it for ROI signal.
- Standup: 09:30 Mon-Fri, 15 min max. Yesterday / Today / Blocked.
- EOD Slack post: bullet list — shipped / broken / blocking.
- Friday retro: 30 min.

## Framework setup (daily flow)

One-time:

- `gsd-ai-integration-phase` → `docs/AI-SPEC.md`. Polish ASR quality bench, consent flow correctness, latency, hallucination prevention on prices. ~1 hour.

Daily:

- Session start: `gsd-resume-work` or read `.continue-here.md`.
- Before new feature: `superpowers:brainstorming`.
- Before multi-step task: `superpowers:writing-plans` → `docs/plans/`.
- During implementation: `superpowers:test-driven-development`.
- Stuck: `superpowers:systematic-debugging`.
- Before merging contract-file changes: `superpowers:requesting-code-review`.
- Session end: `gsd-pause-work`.

Skipped: full `gsd-new-project`, heavy UI design skills, landing-page skills.

## Day 0 action list — status

Completed today:

- [x] `git init` inside this directory.
- [x] Repo structure: `apps/web/`, `apps/backend/`, `packages/contracts/`, `docs/research/`, `docs/plans/`.
- [x] Research docs moved to `docs/research/`, `competitive_analysis.md` typo fixed.
- [x] `CLAUDE.md` written (root). `start_sprint_info.md` superseded and removed.
- [x] `README.md` written.
- [x] `PROJECT_LOG.md` written.
- [x] `.gitignore` written.
- [x] `.DS_Store` removed.
- [x] This plan file written.

Pending — user actions Jenya needs to take:

- [ ] Create GitHub org / private repo. Push this directory.
- [ ] Decide working product name by 18 May (placeholder OK for now).
- [ ] Stand up ElevenLabs workspace on Pro plan ($99/mo). API key in `.env.local`. Training-data opt-out OFF. Audio saving permanently OFF on agents. Apply for ElevenLabs Grants.
- [ ] Stand up Supabase project (Frankfurt). Service-role key in `.env.local`.
- [ ] Stand up Vercel project (`fra1`). Connect repo.
- [ ] Confirm with Sebastian: Booksy partnerships email sent? Pilot 1 identified? Mentors confirmed via Patryk?

Pending — next work (Jenya, today and tomorrow):

- [ ] Run `gsd-ai-integration-phase` → `docs/AI-SPEC.md`.
- [ ] First contract file draft: `voice-agent-provider.ts` + `post-call-webhook.schema.ts` (these unblock everything else).
- [ ] Scraper skeleton in `apps/backend/scraper/` (Playwright + Readability + GPT-4o-mini extraction). Output type from `scraper.schema.ts`.
- [ ] ElevenLabs orchestration skeleton in `apps/backend/orchestration/`: per-clinic agent provisioning via `POST /v1/convai/agents/create`.
- [ ] Polish dental ontology v0: ~30 services, ~20 synonyms, ~10 scripts. YAML in `apps/backend/ontology/`.
- [ ] Server-tool webhook handlers for `check_availability` and `create_booking` (stub Booksy adapter behind interface).
- [ ] Consent flow at the agent's first turn. Default `consent_flag = false` on ambiguous.
- [ ] Post-call webhook receiver gated on `consent_flag`. Stub transcript storage in Supabase.
- [ ] One hardcoded reference clinic to demo internally by end of W1.

## Suggested W1 build sequence (Jenya's solo days, before delegating to Rem)

Day 7 (today): AI-SPEC + contract drafts + scraper skeleton.
Day 8: Orchestration skeleton + ontology v0 + first ElevenLabs agent provisioned for a fake clinic.
Day 9: Server-tool webhooks + post-call webhook receiver + consent flow logic. Internal demo works end-to-end against fake clinic.
Day 10: First real clinic data in scraper, ontology refinement, voice tuning. Hand off to Rem: "here are the contract files, here's the wizard skeleton you can replace, here's the dashboard data model."

## Rem's scope to be defined Day 10-11

Once contract files are locked and Jenya's core demonstrates end-to-end, define Rem's slice precisely:

- Wizard pages and flow (input URL → agent test → optional Booksy paste → optional Twilio number).
- Service value matrix UI bound to `service-value-matrix.schema.ts`.
- Dashboard pages: calls handled, appointments booked, recovered PLN (consumes `appointment-category.enum.ts`).
- SMS short-URL landing page (`.ics` generation, calendar add).
- Booksy adapter v1 (URL/token paste).
- Vercel + Supabase deployment ownership.

Discuss with Rem on Day 10:

- Which integrations beyond Booksy he wants to own end-to-end (Medfile / Estomed / Mediporta / Proassist).
- Whether he owns the SMS pipeline (SMSAPI + short URL + ICS) or just consumes Jenya's events.
- Dashboard polish vs functional v1 trade-off.

## Open status-check items (need user confirmation)

- Booksy partnerships email sent Day 1? (Sebastian)
- ElevenLabs Grants program application submitted? (Jenya)
- Pilot 1 clinic identified or shortlisted? (Sebastian)
- Mentors confirmed via Patryk: Pasha, Piotr (Minerva, not Asystent.ai), Jan?
- GitHub org / private repo created?
